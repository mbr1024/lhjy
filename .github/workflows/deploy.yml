name: Deploy to Server

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          SILICONFLOW_API_KEY: ${{ secrets.SILICONFLOW_API_KEY }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          command_timeout: 30m
          envs: SILICONFLOW_API_KEY,TURSO_DATABASE_URL,TURSO_AUTH_TOKEN
          script: |
            set -e
            
            # 项目目录
            PROJECT_DIR=~/lhjy
            
            echo "=========================================="
            echo "  🚀 开始部署 LHJY（零停机模式）"
            echo "=========================================="
            
            # 1. 进入项目目录并拉取最新代码
            echo "📥 [1/5] 拉取最新代码..."
            cd $PROJECT_DIR
            git fetch origin main
            git reset --hard origin/main
            echo "✅ 代码更新完成"
            
            # 2. 先构建新镜像（旧容器继续运行）
            echo "🔨 [2/5] 构建新 Docker 镜像..."
            docker build -t lhjy-app:new .
            echo "✅ 新镜像构建完成"
            
            # 3. 构建成功后，停止旧容器
            echo "🛑 [3/5] 停止旧容器..."
            docker stop lhjy-app || true
            docker rm lhjy-app || true
            echo "✅ 旧容器已清理"
            
            # 4. 替换镜像标签
            echo "🏷️ [4/5] 更新镜像标签..."
            docker rmi -f lhjy-app:latest || true
            docker tag lhjy-app:new lhjy-app:latest
            docker rmi lhjy-app:new || true
            
            # 5. 启动新容器
            echo "🚀 [5/5] 启动新容器..."
            docker run -d \
              --name lhjy-app \
              --restart always \
              -p 80:3000 \
              -e SILICONFLOW_API_KEY="$SILICONFLOW_API_KEY" \
              -e HOSTNAME="0.0.0.0" \
              -e TURSO_DATABASE_URL="$TURSO_DATABASE_URL" \
              -e TURSO_AUTH_TOKEN="$TURSO_AUTH_TOKEN" \
              --health-cmd="curl -f http://localhost:3000 || exit 1" \
              --health-interval=30s \
              --health-timeout=10s \
              --health-retries=3 \
              lhjy-app:latest
            
            # 6. 等待并验证
            echo "⏳ 等待服务启动..."
            sleep 5
            
            if docker ps | grep -q lhjy-app; then
              echo "=========================================="
              echo "  ✅ 部署成功！"
              echo "=========================================="
              docker ps --filter name=lhjy-app --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
            else
              echo "❌ 部署失败！查看日志："
              docker logs lhjy-app --tail 50
              exit 1
            fi
            
            # 7. 清理无用镜像
            echo "🧹 清理无用镜像..."
            docker image prune -f
            echo "✅ 部署流程完成"
